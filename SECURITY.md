# 安全说明

LUMU 是一个**会真正执行操作的智能体**——它能读写文件、运行终端命令、访问网络。这意味着它的安全模型和普通 Web 应用不同，请务必读完本页再把它暴露到本机以外。

## 一句话安全模型

> **LUMU 的权限 = 运行它的那个系统账号的权限。**
> 谁能访问 LUMU 的接口，谁就约等于拿到了那台机器上该账号的一个 shell。

因此 LUMU 的安全策略**不按用户身份走，而按暴露面走**。

## 三种运行姿态

| 姿态 | 配置 | 鉴权 | 适用 |
|---|---|---|---|
| **本机私用**（默认） | `HOST=127.0.0.1` | 无需口令，打开即用 | 个人日常，99% 的情况 |
| **对外暴露** | `HOST=0.0.0.0` 或经反向代理 | 启动时自动生成一次性口令，必须携带才能访问 | 内网多设备、远程访问 |
| **显式关闭鉴权** | `LUMU_NO_AUTH=1` | 无 —— **任何人可执行任意命令** | 仅限完全隔离的可信网络 |

### 默认是安全的

出厂配置 `HOST=127.0.0.1`，只监听环回地址，外部机器无法连接。此时不需要任何口令，也不会有登录页——这是个人智能体应有的体验。

### 一旦对外，自动上锁

当 LUMU 检测到自己被暴露（绑定非环回地址、位于反向代理之后、或收到非本机来源的请求），会自动启用口令校验：

- 启动时生成 32 字节随机口令，写入 `data/access_token`（权限 0600），并在终端打印带口令的访问链接
- 也可以用环境变量 `LUMU_TOKEN` 指定固定口令
- 口令可通过 `Authorization: Bearer <token>`、`?token=<token>`、Cookie 或 `X-Lumu-Token` 头携带
- 首次带口令访问后会种下 HttpOnly Cookie，同一浏览器后续免输

无口令访问返回 `401`，口令错误返回 `403`。

### 探活端点例外

`/health` 等少量探活端点不校验口令，便于容器编排和监控，它们不返回任何敏感数据、也不触发任何执行能力。

## 强烈建议

1. **不要把 LUMU 直接暴露到公网。** 如果必须远程访问，优先用 VPN、Tailscale、SSH 隧道，而不是开放端口 + 口令。
2. **不要用 root 运行。** 建一个专用低权限账号，只给它需要的目录权限。
3. **生产环境套 HTTPS。** 口令走明文 HTTP 会被中间人截获。
4. **不要开 `LUMU_NO_AUTH=1`。** 除非你完全清楚后果。
5. **定期轮换口令。** 删除 `data/access_token` 后重启即生成新口令。

## 内置防护

- **核心代码写保护**：智能体自身无法修改 `agent/`、`api/`、`providers/` 等核心目录，也不能改 `config.py`、`run.py`、`.env`。扩展能力只能写入 `plugins/`、`skills/`、`knowledge/`。
- **代码沙箱**：代码执行工具在受限环境中运行。
- **人在回路（HITL）**：高危操作可配置为需要人工确认后才执行。
- **出厂零凭据**：仓库和镜像中不含任何 API Key，全部由用户自行配置。

## 你的数据

LUMU 本地优先。对话、记忆、知识库、文件全部存在你自己的 `data/` 目录里，除了你主动配置的模型厂商 API，不向任何第三方发送数据。项目本身不做遥测、不回传统计。

> `data/` 目录包含你的 API Key 和全部对话记录，已在 `.gitignore` 中排除。**不要把它提交到任何仓库，也不要打包分享。**

## 报告漏洞

发现安全问题请**不要**公开提 Issue。请通过 GitHub 私密漏洞报告（仓库 Security 标签页 → Report a vulnerability）提交，包含：

- 问题描述与影响范围
- 复现步骤
- 你认为的严重程度

我们会尽快确认并修复，修复发布后会在 CHANGELOG 致谢（如你愿意署名）。

## 支持版本

项目处于早期快速迭代阶段，安全修复只针对最新发布版本。请保持更新。
