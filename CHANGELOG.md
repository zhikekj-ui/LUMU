# 更新日志

本文件记录 LUMU 的重要变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [0.5.0] — 2026-08-02

首个公开发布版本。这一版的主题是**把智能体交到陌生人手里也能跑起来、也不会出事**。

### 新增

- **能力自省** — 新增 `GET /api/capabilities` 与 `introspect_capabilities` 工具，运行时返回真实的工具清单、已加载技能包、已配置模型厂商、后端路由列表。能力清单不再写死在提示词里，加了工具、装了技能它自己就知道，不会再出现「实际有 139 个工具、自己以为只有 120 个」的认知漂移。
- **技能自创作** — 新增 `skill_pack_write` 工具，智能体可自行编写 `skills/packs/<名字>/SKILL.md` 来扩展自己的能力，写入即热加载生效，下次对话自动进入能力清单。
- **暴露面驱动的访问控制** — 新增 `core/access_guard.py`。本机运行零鉴权、打开即用；一旦对外暴露（绑定非环回地址、位于反向代理之后、或收到非本机请求），自动生成一次性口令并在启动横幅中打印访问链接。支持 Bearer / URL 参数 / Cookie / 自定义头四种携带方式，首次访问后种 HttpOnly Cookie 免输。详见 [SECURITY.md](SECURITY.md)。
- **开源门面** — 补齐 LICENSE（MIT）、CONTRIBUTING、SECURITY、CODE_OF_CONDUCT、CHANGELOG 与 Issue 模板。

### 变更

- **默认监听地址改为 `127.0.0.1`**（原 `.env.example` 示例为 `0.0.0.0`）。个人使用零配置即安全，需要对外时显式改。
- **鉴权逻辑方向修正** — 原实现是「未配置 API_KEY 则全部放行」，方向写反了，在对外部署时等同于完全敞开。现改为「本机放行、对外必须持有口令」。
- 前端重写为 React + Vite + Tailwind + shadcn/ui，源码移入 `webui/`，构建产物由后端在 `api/static/` 同源托管。
- README 移除写死的工具数量，改为指向运行时能力查询。

### 移除

- **删除 `/api/auth/login` 端点及其硬编码凭据。** 该端点是未接入的半成品，且源码中留有明文测试账号密码。个人智能体不做账号体系，此端点不应存在。
- 清理 22 个开发期备份文件（`*.bak*`）、失效的一次性部署脚本（含硬编码服务器地址与本机路径）、两个无人引用的死文件、8 份过期前端构建产物。

### 安全

- 为 `/api/config/channels`、`/api/usage`、`/api/files/{id}` 补齐访问守卫（此前处于无鉴权状态）。
- 验证：85 个端点中 74 个受守卫保护，11 个为有意公开（探活、渠道回调、静态资源）。

---

## [0.4.0] — 2026-08-01

### 新增
- 记忆主动召回增强：真实记忆语义召回常开，替换失效的 intelligent_memory 实现。
- 运行时将当前底层模型与内置能力域注入系统提示，解决「问它用什么模型时瞎猜」的问题。

### 修复
- 文件索引持久化到磁盘，重启后 `/api/files/{fid}` 仍可用，根治刷新后语音／图片／视频卡片消失。

---

## [0.3.0] — 2026-07-31

### 变更
- 移除内置的特定厂商 provider，OpenAI 兼容模式支持自定义 Base URL，任何兼容服务均可接入。

---

## [0.2.0] — 2026-07-30

### 变更
- 精简首次上手引导，移除示例开场板块。

---

## [0.1.0] — 2026-07-29

### 新增
- 首次上手引导（三步引导）。
- RAG 升级为真向量检索（bge-small-zh-v1.5）。
- 知识库加固：修复 FTS5 损坏并新增自愈机制。
- 媒体生成厂商配置接口预留（图像／视频）。
- 出厂默认工具集从 8 个扩展到 21 个。
- 定时任务与心跳机制落地。
- 子智能体任务分派打通。
- 浏览器工具可用化。
- SKILL.md 技能包热加载。

### 性能
- 工具按需暴露（核心常驻 + `tool_find` 激活），系统提示从 15614 token 降至 6407 token（−59%），能力评测分数不变。

### 安全
- 安全底座：HITL 真实拦截、命令沙箱、去 root 运行。
- 封堵模型自我审批漏洞。

---

[0.5.0]: https://github.com/zhikekj-ui/LUMU/releases/tag/v0.5.0
