# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# AI / LLM
openai>=1.50.0
httpx>=0.27.0

# Async / IO
aiofiles>=24.1.0
aiohttp>=3.9.0

# Security / Rate limiting
slowapi>=0.1.10

# Observability
prometheus-client>=0.25.0
structlog>=24.1.0

# DB / Migration
alembic>=1.13.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Optional channels
discord.py>=2.3.0
python-telegram-bot>=20.0

# --- Runtime deps required for full features (auth, DB, KB, upload) ---
asyncpg>=0.29.0
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1
email-validator>=2.1.0
fastembed>=0.3.0
pypdf>=4.0.0
python-docx>=1.1.0
jieba>=0.42.1
python-multipart>=0.0.9

# Browser automation (Playwright — 浏览器工具核心依赖；Docker 镜像内一并安装 Chromium)
# 版本与开发/演示服务器对齐（1.61.0），保证干净安装也能获得浏览器能力。
playwright==1.61.0

# Desktop control — 控制电脑是 LUMU 标配能力（截屏/鼠标/键盘/窗口查询）。
# 随分发安装，macOS/Windows 桌面环境开箱即用；Linux 无 GUI 不需也装不上 X11 依赖，
# 故按平台跳过 —— 代码内 _AVAILABLE 已对缺失依赖优雅降级为清晰提示，不影响服务启动。
pyautogui>=0.9.54; sys_platform != "linux"
Pillow>=10.0.0
